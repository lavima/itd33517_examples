height,width = image.shape

out = numpy.zeros(image.shape, dtype=numpy.complex)
for u in range(0,height):
  for v in range(0,width):
    for i in range(0,height):
      for j in range(0,width):
        skriv inn matematisk uttrykk
